# Production environment variables
# REST API is proxied through Vercel to maintain same-origin for iOS compatibility
VITE_API_URL=/api

# Note: WebSocket connections go directly to Heroku (Vercel doesn't support WebSocket proxying)
# WebSocket uses token exchange pattern: fetch short-lived token via REST, then connect directly
VITE_BACKEND_WS_URL=wss://quipflip-c196034288cd.herokuapp.com
