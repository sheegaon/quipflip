# Production environment variables
# REST API is proxied through Vercel to maintain same-origin for iOS compatibility
VITE_API_URL=/api

# WebSocket connection goes directly to Heroku (cannot be proxied)
# Cross-site cookies are handled via SameSite=None with Secure flag
VITE_WEBSOCKET_URL=wss://quipflip-c196034288cd.herokuapp.com
