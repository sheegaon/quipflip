# MemeMint Frontend Environment Variables

# Backend REST API URL
# Development: http://localhost:8000 (direct to local backend)
# Production: same-origin host (leave unset to use window.location.origin)
VITE_API_URL=http://localhost:8000

# WebSocket URLs are derived from the same API base in development and from
# window.location in production.
