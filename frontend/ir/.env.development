# Development environment variables
# REST API connects directly to local backend with /ir prefix
VITE_API_URL=http://localhost:8000/ir

# WebSocket also connects directly to local backend
# Uses token exchange pattern: fetch short-lived token via REST, then connect with token
VITE_BACKEND_WS_URL=ws://localhost:8000
